
```diff
diff --git a/x86/syscall.c b/x86/syscall.c
index 8cef860..e084435 100644
--- a/x86/syscall.c
+++ b/x86/syscall.c
@@ -5,6 +5,13 @@
 #include "msr.h"
 #include "desc.h"

+static u64 current_cycles(void)
+{
+       u32 low, high;
+       asm volatile("rdtsc" : "=a"(low), "=d"(high));
+       return ((u64)low) | ((u64)high << 32);
+}
+
 static void test_syscall_lazy_load(void)
 {
     extern void syscall_target(void);
@@ -100,8 +107,12 @@ static void test_syscall_tf(void)

 int main(int ac, char **av)
 {
+    u64 start, end;
+    start = current_cycles();
     test_syscall_lazy_load();
     test_syscall_tf();
+    end = current_cycles();
+    printf("cost %lu CPU cycles\n", (end - start));

     return report_summary();
 }
```